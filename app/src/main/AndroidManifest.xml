<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.app.mgxchange">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MGXChange"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon">
        <activity android:name=".activities.UserUpdateProfile"></activity>
        <activity android:name=".activities.ItemListDetails" />
        <activity android:name=".activities.ComplainListDetails" />
        <activity android:name=".activities.ComplainList" />
        <activity android:name=".activities.AddProductForLoan" />
        <activity android:name=".activities.ItemsList" />
        <activity android:name=".activities.DetailedUserProfile" />
        <activity android:name=".activities.SellProduct" />
        <activity android:name=".activities.GetLoanUsingProduct" />
        <activity android:name=".activities.LoanOrSellSelection" />
        <activity android:name=".activities.AddComplain" />
        <activity android:name=".activities.Welcome" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ForgetPassword" />
        <activity android:name=".activities.UserSignUp" />
        <activity android:name=".activities.UserLogin" />
        <activity android:name=".activities.PrivacyPolicy" />
        <activity
            android:name=".activities.Dashboard"
            android:label="@string/title_activity_dashboard" />
    </application>

</manifest>